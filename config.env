# Monitor Control HTTP API Configuration
# This file should be placed in the same directory as monitor_control_gui.exe

# HTTP server port (default: 45678)
HTTP_PORT=45678

# HTTP server host (default: 127.0.0.1 for localhost-only access)
# Use 0.0.0.0 to allow external connections (not recommended without authentication)
HTTP_HOST=127.0.0.1

# Enable or disable the HTTP API server
# Values: true, false, 1, 0, yes, no, on, off
API_ENABLED=true
